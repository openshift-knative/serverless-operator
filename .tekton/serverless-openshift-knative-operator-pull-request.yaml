apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  annotations:
    build.appstudio.openshift.io/repo: https://github.com/openshift-knative/serverless-operator?rev={{revision}}
    build.appstudio.redhat.com/commit_sha: '{{revision}}'
    build.appstudio.redhat.com/pull_request_number: '{{pull_request_number}}'
    build.appstudio.redhat.com/target_branch: '{{target_branch}}'
    pipelinesascode.tekton.dev/max-keep-runs: "3"
    pipelinesascode.tekton.dev/on-cel-expression: event == "pull_request" && target_branch
      == "konflux" && files.all.exists(x, !x.matches('^olm-catalog/|^knative-operator/'))
  creationTimestamp: null
  labels:
    appstudio.openshift.io/application: serverless-operator-konflux
    appstudio.openshift.io/component: serverless-openshift-knative-operator
    pipelines.appstudio.openshift.io/type: build
  name: serverless-openshift-knative-operator-on-pull-request
  namespace: ocp-serverless-tenant
spec:
  params:
  - name: dockerfile
    value: openshift-knative-operator/Dockerfile
  - name: git-url
    value: '{{source_url}}'
  - name: image-expires-after
    value: 5d
  - name: output-image
    value: quay.io/redhat-user-workloads/ocp-serverless-tenant/serverless-operator-konflux/serverless-openshift-knative-operator:on-pr-{{revision}}
  - name: revision
    value: '{{revision}}'
  pipelineRef:
    name: serverless-openshift-knative-operator
  taskRunTemplate: {}
  workspaces:
  - name: workspace
    volumeClaimTemplate:
      metadata:
        creationTimestamp: null
      spec:
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: 1Gi
      status: {}
  - name: git-auth
    secret:
      secretName: '{{ git_auth_secret }}'
status: {}
