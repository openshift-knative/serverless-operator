apiVersion: v1
kind: Service
metadata:
  labels:
    name: knative-operator
  name: operator-webhook
spec:
  ports:
    - port: 443
      targetPort: 8443
  selector:
    name: knative-operator
  type: ClusterIP
