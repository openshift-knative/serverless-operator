apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
  namespace: {{ .namespace }}
data:
  {{ if .channel }}
  channel-template-spec: |
    apiVersion: messaging.knative.dev/{{ .channel.version }}
    kind: Channel
  {{ end}}
