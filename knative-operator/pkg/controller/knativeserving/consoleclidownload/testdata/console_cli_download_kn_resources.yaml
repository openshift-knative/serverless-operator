apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: kn-cli-downloads
  namespace: knative-serving
spec:
  template:
    spec:
      containers:
      - name: kn-download-server
        image: IMAGE_KN_CLI_ARTIFACTS
status:
  url: knroute.example.com
  conditions:
  - status: "True"
    type: Ready

